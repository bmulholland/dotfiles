. "$HOME/.cargo/env"

export RUBY_YJIT_ENABLE=1

eval "$(/Users/bmulholland/.local/bin/mise activate zsh)"

# I use a pager for normal terminal use, but it breaks Cursor's terminal
if [[ "$TERM_PROGRAM" == "vscode" ]]; then
  export PAGER=cat
  export GIT_PAGER=cat
fi
