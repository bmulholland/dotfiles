[tools]
node = "24.7.0"
python = "3.13.7"
ruby = "3.4.5"
usage = "latest"
terraform = "latest"
helm = "latest"

[env]
# Removed automatic venv activation to avoid warnings in non-Python projects
# Use project-specific .mise.toml files with _.python.venv for Python projects that need it

[settings]
idiomatic_version_file_enable_tools = ["python", "ruby"]
